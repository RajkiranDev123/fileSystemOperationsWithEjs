<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Document</title>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-yellow-900">
      <div class="form p-10">
        <form action="/create" method="POST">
          <input
            class="w-full rounded-md outline-none block px-5 py-5 bg-white-500"
            type="text"
            placeholder="title..."
            name="title"
          />
          <textarea
            class="block w-full rounded-md outline-none mt-2 px-5 py-5 bg-white-500 resize-none"
            placeholder="task details..."
            name="details"
          ></textarea>
          <input
            class="mt-2 bg-blue-600 px-5 py-5 rounded-md cursor-pointer text-white"
            type="submit"
            value="Create task"
          />
        </form>
      </div>

      <div class="task p-10 flex gap-3 flex-wrap">
        <% if(files.length>0){ %> <% files.forEach(function(val){%>
        <div class="task min-w-72 px-3 py-4 rounded-md bg-zinc-800">
          <h1 class="text-white text-3xl tracking-tighter"><%= val %></h1>

          <div class="flex w-full justify-between items-center">
            <a class="text-blue-500 initial-block mt-3" href="/file/<%=val%>"
              >Read more...</a
            >
            <a class="text-blue-500 initial-block mt-3" href="/edit/<%=val%>"
              >Edit</a
            >
          </div>
        </div>
        <% }) %> <% }else{ %>
        <h1 class="text-white">No tasks yet...</h1>
        <% } %>
      </div>
    </div>
    <script src="/javascripts/script.js"></script>
  </body>
</html>
